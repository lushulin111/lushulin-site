# 2026-07-29

## 00:14 — 联系方式板块改为截图样式

- 按用户截图将「联系我」简化为：小标签「保持联系」+ 邀请文案 + 观赏性「我的邮箱」按钮 + 邮箱地址 `lushulin98@qq.com` + 「通常在 <strong>24 小时内</strong>回复」。
- `index.html`：将按钮改为无 `href` 的 `<span>`，彻底不再跳转。
- `script.js`：移除残留的 `initContactModal()` 函数与调用。
- `style.css`：清理之前遗留的 `.contact-form`、`.contact-input-row`、`.contact-submit`、`.contact-modal-*` 等不再使用的样式。
- 已重新部署 CloudStudio，分享链接不变。

## 13:06 — 图标个性化 + 板块间距优化 + 动画个性化

**需求**：不改动文本和主题色，① emoji 图标更个性化；② 板块间距/联动提升视觉舒适度；③ 小组件动画不死板。

**图标个性化（data.js）**：
- 核心能力：🎯→💎（高客单价）、🚀→🔧（SOP 设计）、📚→📐（课程研发）、👥→🧬（团队复制）、📈→🎛️（ROI 优化）、✍️→✒️（内容文案）
- 工作旅程节点：📸→📷、🎙️→🌐、💎→📺、🧩→🧱、🌱→🚣
- `.capability-icon`：从纯文字 emoji 升级为 56×56 渐变背景圆角容器，hover 时缩放+旋转

**间距优化（style.css）**：
- `.section` padding 80px→96px；`.section-header` margin-bottom 56px→64px
- `.capabilities-grid` gap 24px→28px；`.capability-card` padding 32px→34px 32px
- `.stats-grid` gap 16px→20px；`.journey-item` margin-bottom 56px→64px
- `.capability-note` margin 28px→32px

**动画个性化（style.css）**：
- 核心能力卡片：统一 slideInUp → `cardSlideIn`（scale+translateY），按 `--card-i` 错峰 0.08s
- 工作旅程：左卡片 `slideInLeft`（从左滑入）、右卡片 `slideInRight`（从右滑入），按 `--journey-i` 错峰 0.1s
- 数据卡片：`statPop`（缩放弹入），按 `--stat-i` 错峰 0.1s
- 工作旅程节点：`nodeBreathe`（呼吸缩放）+ `nodeRipple`（涟漪扩散）无限循环
- 核心能力卡片 hover：`::after` 渐变光扫效果
- 联系按钮：`contactFloat` 轻微上下浮动
- 工作旅程列表项 hover：`padding-left` 微移 + 圆点放大
- 数据卡片 hover：底部渐变条 `::after` 横向展开
- 新增 `@media (prefers-reduced-motion: reduce)` 无障碍降级

**script.js**：renderHeroStats/renderCapabilities/renderJourney 均添加 `--stat-i`/`--card-i`/`--journey-i` CSS 变量供错峰动画使用；journey-item 加 `.reveal` 类。

已重新部署 CloudStudio，分享链接不变。
